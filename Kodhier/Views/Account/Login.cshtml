@model LoginViewModel
@inject SignInManager<ApplicationUser> SignInManager
@using Microsoft.AspNetCore.Mvc.Localization
@inject IViewLocalizer Localizer
@{
    ViewData["Title"] = Localizer["Log in"];
}
<link rel="stylesheet" type="text/css" href="~/css/LogInPage.css" />

<div class="main">
    <form asp-route-returnurl="@ViewData["ReturnUrl"]" method="post" id="login-box">
        <header>
            <img src="~/images/icon.png">
            <h2>@ViewData["Title"]</h2>
        </header>

        <div class="form-group">
            <label asp-for="Username">@Localizer["Username"]</label>
            <input asp-for="Username" class="form-control" type="text" placeholder=@Localizer["Enter Username"] />
            <span asp-validation-for="Username" class="text-danger"></span>
        </div>
        <div class="form-group">
            <label asp-for="Password">@Localizer["Password"]</label>
            <input asp-for="Password" class="form-control" placeholder=@Localizer["Enter Password"] />
            <span asp-validation-for="Password" class="text-danger"></span>
        </div>
        <div class="form-group">
            <div>
                <label asp-for="RememberMe">
                    <input asp-for="RememberMe" />
                    @Localizer["Remember me?"]
                </label>
            </div>
        </div>
        <div class="form-group">
            <button type="submit" class="btn">@Localizer["Log in"]</button>
        </div>
        <div class="form-group">
            <a asp-action="ForgotPassword" class="backToMeniubtn">@Localizer["Forgot your password?"]</a>
            <a asp-action="Register" asp-route-returnurl="@ViewData["ReturnUrl"]" class="backToMeniubtn">@Localizer["Register as a new user?"]</a>
        </div>
    </form>
</div>
@section Scripts {
    @await Html.PartialAsync("_ValidationScriptsPartial")
}
